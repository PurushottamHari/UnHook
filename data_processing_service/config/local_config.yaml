service:
  service_name: "data_processing_service"
  service_port: 8002

external:
  user_service:
    base_url: "http://user_service"
    port: 8000
    timeout: 120.0 # 2 minutes timeout

redis:
  host: "redis"
  port: 6379
  db: 0

s3:
  account_id: "minioadmin"
  bucket_name: "unhook-ephemeral"
  endpoint_url: "http://minio:9000"

messaging:
  commands:
    data_collection_service:
      topic: "data_collector_service.commands"
    data_processing_service:
      topic: "data_processing_service.commands"

  events:
    data_processing_service:
      topic: "data_processing_service.events"
    data_collection_service:
      topic: "data_collection_service.events"
  events:
    data_processing_service:
      topic: "data_processing_service.events"
