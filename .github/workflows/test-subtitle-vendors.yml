name: Test Subtitle Vendors

on:
  workflow_dispatch:  # Only manual triggering

jobs:
  test-subtitle-vendors:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11.13'
        
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r download_subtitles_test/requirements-subtitle-test.txt
        
    - name: Run yt-dlp proxy tests
      run: |
        echo "🚀 Starting yt-dlp proxy tests..."
        python download_subtitles_test/test_ytdlp_proxy.py
      env:
        ZYTE_API_KEY: ${{ secrets.ZYTE_API_KEY }}
